export interface SupportPanel {
    title: string;
    img: string;
    url: string;
    button: string;
    text: string;
}

export interface SupporterPanel {
    name: string;
    img: string;
    url: string;
}