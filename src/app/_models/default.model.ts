export interface TitleTextPanel {
    title: string;
    text: string;
}

export interface DefaultPanel {
    title: string;
    text: string;
    img: string;
}

export interface ParagraphsPanel {
    title: string;
    text: [];
    img: string;
}